name: Build hr service

on:
  push:
    branches:
      - main

    paths:
      - "build.gradle.kts"
      - "hr/**"
      - ".github/workflows/build-hr.yaml"
      - ".github/workflows/build.yaml"
      - ".github/workflows/deploy.yaml"
      - "ci.Dockerfile"

jobs:
  build:
    name: Build Jar and Dockerize
    uses: ./.github/workflows/build.yaml
    secrets: inherit
    with:
      service: hr
